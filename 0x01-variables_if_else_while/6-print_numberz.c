#include <stdio.h>

/**
 * main -C programm eEntry point
 * Return: Always zero(0) on sucess
 */
int main(void)
{
	int j = 0;

	while (j < 10)
	{
		putchar(j);
		j++;
	}
	putchar('\n');
	return (0);
}
