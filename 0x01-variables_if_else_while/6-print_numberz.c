#include <stdio.h>

/**
 * main -C programm eEntry point
 * Return: Always zero(0) on sucess
 */
int main(void)
{
	int i = 48;

	while (i < 58)
	{
		putchar(i);
		i++;
	}
	putchar('\n');
	return (0);
}
